"""hw4.sdk — Public SDK layer.

All business logic is exposed through this module so external callers
never import internal implementation modules directly.

Populated in Phase 2+ as modules are implemented.
"""
