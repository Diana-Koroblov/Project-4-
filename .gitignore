# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets — never commit real credentials
.env
*.key
*.pem
credentials.json

# Local config override — each developer has their own
# Copy config/setup.example.json → config/setup.json and edit locally
config/setup.json
